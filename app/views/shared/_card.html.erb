<%= link_to article_path(item), class: "card" do %>
    <h2><%= item.title %></h2>
    <span class="preview-text">
        <%= item.body[..256] %>...
    </span>
    <span class="author">
        Author: <%= item.user.username %>
    </span>
<% end %>
